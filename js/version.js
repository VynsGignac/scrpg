// ============================================================
// NUMERO DE VERSION
// Le dernier chiffre (patch) est incremente AUTOMATIQUEMENT par build-apk.ps1 (avant chaque
// build d'APK) et par publish-web.ps1 (a chaque vraie publication web, meme mecanisme que le
// CACHE_NAME de sw.js) ; les deux premiers (majeur.mineur) ne changent que sur demande
// explicite de l'utilisateur.
// ============================================================

const GameVersion = '0.1.083';
